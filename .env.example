# Node server
PORT=3000

# Runtime directories (absolute path recommended for production)
DATA_DIR=./data
HF_HOME=./.cache/huggingface

# Python runtime
# If not set, server will auto use .venv/bin/python when available.
PYTHON_EXECUTABLE=./.venv/bin/python
# TRANSCRIBE_SCRIPT=./scripts/transcribe_senko.py

# Speaker-turn merge behavior
MERGE_MAX_GAP_SECONDS=15
MERGE_MAX_CHARS=1200
